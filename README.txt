Description: 

This modules sets up a connection between a Drupal site and a mixpanel instance via a mixpanel token. This allows for event tracking information being sent to mixpanel.

Configuration route:

/admin/config/cornell_mixpanel

More to come...
